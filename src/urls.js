
const COMMON = {
	// All common urls will goes here Ex: end points for getting orders data for both farmers and consumers
};

const GUEST = {
	login: 'validateLogin.do/', // Login api
	signup: 'registration/', // Registration api
	config: 'config/', // Config api
};

const FARMER = {
};

const CONSUMER = {
};

const ADMIN = {
};

export default{
	GUEST,
	FARMER,
	CONSUMER,
	ADMIN
}